# MEMORY.md — project memory index

This is your project's long-term memory index. One line per memory below. When you (or Claude Code) learn something non-obvious worth keeping, add a one-line pointer here and the full fact in your memory store (or expand inline if you're keeping it simple).

The START-HERE instruction seeds your first few entries during setup. Keep entries to one fact each; don't store what the code or git history already records.

<!-- Example format (delete once you have real entries):
- **windows-npx-mcp** — On Windows, npx-based MCP servers only start when wrapped with `cmd /c`.
- **node-pinned-20** — Local + Netlify both pinned to Node 20 to avoid deploy mismatches.
-->
