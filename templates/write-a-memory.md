# Task template — Write a memory

Paste this into Claude Code when you've learned something worth keeping.

---

I learned something I don't want to rediscover later. Save it as a memory using the kit's pattern (one fact per entry, short description, in my memory store or `MEMORY.md`).

**The thing I learned:**
> _e.g. "On Windows, the Notion MCP server only starts if wrapped with `cmd /c`."_

**Why it matters / when it'll come up again:**
> _e.g. "Every time I set up a new project on this machine."_

Please:
1. Decide if this is genuinely worth saving — skip it if the code or git history already records it. Tell me if you're skipping and why.
2. If worth it, write a single memory entry: a kebab-case name, a one-line description, and the fact itself. Add a one-line pointer to the `MEMORY.md` index.
3. Link it to any related memory if one exists.
4. Confirm what you saved in one line.
